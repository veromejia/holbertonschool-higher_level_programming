The `1-`my_list`` module
======================

Using ``my_list``
-------------------

Import Class:
>>> MyList = __import__('1-my_list').MyList

Now test it:
    >>> my_list = MyList()
    >>> print(my_list)
    []

    >>> my_list.append(2)
    >>> my_list.append(1)
    >>> print(my_list)
    [2, 1]

    >>> my_list.print_sorted()
    [1, 2]

    >>> my_list.append(-5)
    >>> my_list.print_sorted()
    [-5, 1, 2]

    >>> my_list = MyList("hello")
    >>> my_list
    ['h', 'e', 'l', 'l', 'o']

    >>> my_list.print_sorted()
    ['e', 'h', 'l', 'l', 'o']
